// 用于配置gradle全局参数的配置文件
ext {
    BUILD_TOOLS_VERSION = "26.0.2"

    COMPILE_SDK_VERSION = 26
    MIN_SDK_VERSION = 16
    TARGET_SDK_VERSION = 25

    // 支持库版本全局配置
    APP_COMPAT_V7_VERSION = "27.1.0"
    RECYCLERVIEW_V7_VERSION = APP_COMPAT_V7_VERSION
    CARDVIEW_V7_VERSION = APP_COMPAT_V7_VERSION
    GRID_LAYOUT_V7_VERSION = APP_COMPAT_V7_VERSION
    DESIGHT_VERSION = APP_COMPAT_V7_VERSION

    SUPPORT_V4_VERSION = "27.1.0"
}